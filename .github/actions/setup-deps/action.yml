name: 'Setup Dependencies'
description: 'Sets up Node.js, Ruby, and optionally Playwright with caching'
# v2: adds skip-system-deps for Playwright browser-only installs

inputs:
  playwright-browser:
    description: 'Playwright browser to install (chromium, firefox, webkit, or empty to skip)'
    required: false
    default: ''
  include-ruby:
    description: 'Whether to set up Ruby'
    required: false
    default: 'true'
  prebuild-js:
    description: 'Run npm run prebuild:js to copy lunr.min.js'
    required: false
    default: 'false'
  skip-system-deps:
    description: 'Skip playwright install-deps (apt-get). Use when the browser binary alone is sufficient and system packages are already present (e.g. Lighthouse headless_shell).'
    required: false
    default: 'false'

runs:
  using: 'composite'
  steps:
    - name: Setup Node.js
      uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6
      with:
        node-version-file: '.node-version'
        cache: 'npm'

    - name: Install Node dependencies
      shell: bash
      run: npm ci

    - name: Prebuild JS (copy lunr.min.js)
      if: inputs.prebuild-js == 'true'
      shell: bash
      run: npm run prebuild:js

    - name: Setup Ruby
      if: inputs.include-ruby == 'true'
      uses: ruby/setup-ruby@89f90524b88a01fe6e0b732220432cc6142926af # v1.313.0
      with:
        bundler-cache: true

    - name: Cache Jekyll build
      if: inputs.include-ruby == 'true'
      uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5
      with:
        path: |
          .jekyll-cache
          .jekyll-metadata
        key: ${{ runner.os }}-${{ runner.arch }}-jekyll-${{ hashFiles('_config.yml', 'Gemfile.lock') }}
        restore-keys: |
          ${{ runner.os }}-${{ runner.arch }}-jekyll-

    - name: Cache Playwright Browsers
      if: inputs.playwright-browser != ''
      id: playwright-cache
      uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5
      with:
        path: ~/.cache/ms-playwright
        key: ${{ runner.os }}-${{ runner.arch }}-playwright-${{ inputs.playwright-browser }}-${{ hashFiles('package-lock.json') }}

    - name: Install Playwright browsers
      if: inputs.playwright-browser != ''
      shell: bash
      run: |
        if [ "${{ inputs.skip-system-deps }}" = "true" ]; then
          # Binary only — no apt-get. Caller guarantees system deps are present.
          npx playwright install ${{ inputs.playwright-browser }}
        elif [ "${{ steps.playwright-cache.outputs.cache-hit }}" = "true" ]; then
          # Binary cached; still need system packages (fonts, libs) on the runner.
          npx playwright install-deps ${{ inputs.playwright-browser }}
          npx playwright install ${{ inputs.playwright-browser }}
        else
          npx playwright install --with-deps ${{ inputs.playwright-browser }}
        fi
